- name: psql
  require:
    name: database
    help: Database name
  command: psql
  env:
    - name: PGPASSWORD
      value: "{{ password }}"
    - name: PGCHANNELBINDING
      value: disable
  args: -h {{ host }} -U {{ user }} -d {{ database }} --port {{ port }}
